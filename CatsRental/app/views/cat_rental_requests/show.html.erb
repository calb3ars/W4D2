<h1> You have booked <%= @cat_rental_requests.cat.name %> </h1>

<table>
  <tr>
    <td>Cat: </td>
    <td><%= @cat_rental_requests.cat.name %></td>
  </tr>
  <tr>
    <td>Start Date: </td>
    <td><%= @cat_rental_requests.start_date %></td>
  </tr>
  <tr>
    <td>End Date: </td>
    <td><%= @cat_rental_requests.end_date %></td>
  </tr>
</table>

<%= button_to "Approve", {action: "approve"} %>
<%= button_to "Deny", {action: "deny"} %>
